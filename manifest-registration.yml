applications:
- name: tracker-registration
  path: ./applications/registration-server/build/libs/registration-server.jar
  routes:
  - route:  registration.apps.pcf.paltraining.perficient.com
  memory: 1G
  instances: 1
  env:
  services:
  - tracker-registration-database
